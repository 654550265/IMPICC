import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FramSpecPage } from './fram-spec';

@NgModule({
  declarations: [
    FramSpecPage,
  ],
  imports: [
    IonicPageModule.forChild(FramSpecPage),
  ],
})
export class FramSpecPageModule {}
