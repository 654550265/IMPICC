import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { NchangePassWordPage } from './nchange-pass-word';

@NgModule({
  declarations: [
    NchangePassWordPage,
  ],
  imports: [
    IonicPageModule.forChild(NchangePassWordPage),
  ],
})
export class NchangePassWordPageModule {}
